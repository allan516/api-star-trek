{
  "name": "api-star-trek",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start:dev": "tsx --env-file=.env src/server.ts",
    "start:watch": "tsx watch --env-file=.env src/server.ts",
    "build": "tsup --env-file=.env src",
    "start:build": "npm run build && node dist/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "The Star Trek API is a backend application that provides information about characters, ships and planets.",
  "devDependencies": {
    "@types/express": "^5.0.0",
    "tsup": "^8.3.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
